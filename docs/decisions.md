# 決策紀錄

記錄「為什麼這樣做」，以及**決定不做、延後、或被否決的方案**——後者最容易在對話中流失，
三個月後回來又得重新推導一次。

「改了什麼」的時間軸請見 [`CHANGELOG.md`](../CHANGELOG.md)。
實作規格請見 [`.spec/specs/`](../.spec/specs/)。

格式：每則記錄 **背景 → 決定 → 理由 → 否決的方案 → 重新評估的條件**。

## 目錄

依主題分組；編號是撰寫順序，不是閱讀順序。

**爬蟲與平台限制** — 抓不到資料時，先確定擋你的是什麼。

- [1. 蝦皮瀏覽器爬蟲不在 CI 上執行](#1-蝦皮瀏覽器爬蟲不在-ci-上執行)
- [2. 蝦皮官方資料管道暫不可用](#2-蝦皮官方資料管道暫不可用)
- [42. 蝦皮要的是「最近有人通過驗證」，不是 session，也不是瀏覽器模式](#42-蝦皮要的是最近有人通過驗證不是-session也不是瀏覽器模式)
- [20. 旋轉拍賣移到本機執行](#20-旋轉拍賣移到本機執行)
- [21. 429 是兩種問題共用一個狀態碼](#21-429-是兩種問題共用一個狀態碼)
- [6. Gemini 維持 3.5 Flash Lite](#6-gemini-維持-35-flash-lite)
- [18. 解析成本必須與新物件數量掛鉤，不是與資料庫總量](#18-解析成本必須與新物件數量掛鉤不是與資料庫總量)

**解析與資料模型** — 抽取寧可缺值，不可猜測。

- [9. 規格抽取寧可缺值，不可猜測](#9-規格抽取寧可缺值不可猜測)
- [15. 支援 MacBook Neo（A18 Pro）—— 寫死家族的老問題換個形狀再犯](#15-支援-macbook-neoa18-pro-寫死家族的老問題換個形狀再犯)
- [13. 瑕疵判定移到 pipeline，結果隨規格一起存](#13-瑕疵判定移到-pipeline結果隨規格一起存)
- [3. 瑕疵品標註而不扣分、不隱藏](#3-瑕疵品標註而不扣分不隱藏)
- [23. 規則改變時，已存的資料不會自己跟上](#23-規則改變時已存的資料不會自己跟上)
- [38. 支援非 MacBook 的 Mac —— 延後，卡點是資料模型不是資源](#38-支援非-macbook-的-mac--延後卡點是資料模型不是資源)
- [40. 一個標題同時出現 Apple 晶片與 Intel 世代訊號，就不是一台機器](#40-一個標題同時出現-apple-晶片與-intel-世代訊號就不是一台機器)
- [41. 賣場範本的分級說明不算機況](#41-賣場範本的分級說明不算機況)
- [43. 標題級的否決必須獨立於「晶片從哪裡來」](#43-標題級的否決必須獨立於晶片從哪裡來)

**資料庫語意** — 欄位的意義由誰有資格寫入決定。

- [4. 下架判定採時間老化，不採「本次未見」](#4-下架判定採時間老化不採本次未見)
- [16. `last_seen` 只能由「真的看到」來更新](#16-last_seen-只能由真的看到來更新)
- [17. 修復條件不得由修復動作自己滿足](#17-修復條件不得由修復動作自己滿足)

**評分與呈現** — 分數要能被讀的人驗算。

- [7. 前後端 VFM 公式不一致](#7-前後端-vfm-公式不一致)
- [25. 色帶基準取自全站，而且要說出來](#25-色帶基準取自全站而且要說出來)
- [26. 說明區只做一件事：讓分數可被驗算](#26-說明區只做一件事讓分數可被驗算)
- [22. 列表改版：結果列，機型當大字](#22-列表改版結果列機型當大字)
- [24. 網站更名：不做的事就不要寫在標題上](#24-網站更名不做的事就不要寫在標題上)
- [5. 行情價差顯示 —— 延後](#5-行情價差顯示--延後)
- [39. 支援 Mac mini 與 Mac Studio：同一份公式，分開的級距](#39-支援-mac-mini-與-mac-studio同一份公式分開的級距)

**測試、CI 與工程紀律** — 只驗會默默壞掉的東西。

- [10. 測試不得依賴外部服務](#10-測試不得依賴外部服務)
- [11. Linter 規則寧可少而準](#11-linter-規則寧可少而準)
- [12. 文件與程式碼的一致性交給 CI 檢查](#12-文件與程式碼的一致性交給-ci-檢查)
- [14. 瀏覽器測試：只驗會默默壞掉的東西](#14-瀏覽器測試只驗會默默壞掉的東西)
- [19. 可執行的腳本，不是要人照著重打的註解](#19-可執行的腳本不是要人照著重打的註解)
- [30. CI 的依賴清單不再手寫](#30-ci-的依賴清單不再手寫)
- [37. logging 設定集中到單一模組](#37-logging-設定集中到單一模組)

**維運與失敗通報** — 一個不會出聲的失敗等於沒有失敗。

- [27. 一個 bug 的兩層：壞掉的，和慢到像壞掉的](#27-一個-bug-的兩層壞掉的和慢到像壞掉的)
- [28. 通報者不能活在被通報的東西裡面](#28-通報者不能活在被通報的東西裡面)
- [29. 共用步驟不能只用被移除者的理由來刪](#29-共用步驟不能只用被移除者的理由來刪)
- [33. Postgres 連線要設逾時，不能信任 OS 預設值](#33-postgres-連線要設逾時不能信任-os-預設值)
- [35. Streamlit 休眠的對策要真的打開瀏覽器，curl 只是看起來有用](#35-streamlit-休眠的對策要真的打開瀏覽器curl-只是看起來有用)

**公開與隱私** — 提交出去就收不回來。

- [8. 公開前的資料衛生稽核](#8-公開前的資料衛生稽核)
- [31. 公開改用新 repo，而不是改寫舊 repo 後強推](#31-公開改用新-repo而不是改寫舊-repo-後強推)
- [32. Streamlit app 的公開設定，與 repo 的公開設定是兩回事](#32-streamlit-app-的公開設定與-repo-的公開設定是兩回事)
- [34. CI 探測腳本不再把蝦皮 session 打包成公開 artifact](#34-ci-探測腳本不再把蝦皮-session-打包成公開-artifact)
- [36. 公開授權採用 MIT License](#36-公開授權採用-mit-license)

---

## 1. 蝦皮瀏覽器爬蟲不在 CI 上執行

**日期**：2026-08-27　**狀態**：已確認（實測）

**背景**：每日雲端排程的蝦皮資料長期為 0 筆。

**決定**：蝦皮瀏覽器爬蟲只在住宅／行動網路執行，CI 只跑 PTT 與 Carousell。

**理由**：原先列出四個原因，實測後區分為：

| 原因 | 可否在 CI 解決 |
|---|---|
| session 未帶到 runner | ✅ 可（存成 secret） |
| headless 撞登入牆回傳空 list | ✅ 可 |
| camoufox 瀏覽器核心未安裝 | ✅ 可（`camoufox fetch`） |
| **蝦皮判定 runner IP 為爬蟲** | ❌ **不可** |

補齊前三項後實測，runner 帶著 22 個有效 cookie 仍被導向
`shopee.tw/verify/captcha?...&scene=crawler_item`。`scene=crawler_item` 是蝦皮自己的分類標記；
**session 有效卻仍被攔，代表判斷依據是執行環境而非憑證**。

**否決**：改用其他免費雲端（Oracle Always Free 等）。稀缺的不是運算資源，是非機房 IP。

**重新評估**：`.github/workflows/shopee-ci-test.yml` 可手動觸發重驗，不寫入資料庫。

---

## 2. 蝦皮官方資料管道暫不可用

**日期**：2026-08-27　**狀態**：等待對方開通

**決定**：Affiliate API 的程式已完成並保持啟用條件，但目前走瀏覽器爬蟲。

**理由**：分潤計畫審核通過 ≠ 有 API 權限。後台顯示
「您目前無權限申請蝦皮分潤計畫之Open API金鑰。請聯繫我們以開通權限」，
按鈕為停用狀態。Product Feed 同樣顯示「尚無數據」，兩條官方路徑都需對方開通。

**重新評估**：取得金鑰後先執行 `probe_shopee_affiliate` 驗證二手個人賣家的覆蓋率，
覆蓋率不足則維持現狀。

---

## 3. 瑕疵品標註而不扣分、不隱藏

**日期**：2026-08-27　**狀態**：已實作

**背景**：VFM 獎勵低價，而壞掉的機器正因為壞掉才便宜，因此瑕疵品必然浮到榜首。
實測分數最高的兩筆分別是「瑕疵機」與「外接機」（螢幕已壞）。

**決定**：偵測並標註，不介入評分，另提供隱藏開關。

**理由**：瑕疵該折價多少沒有客觀答案，任何懲罰係數都是憑空訂的，會污染整個評分基準。
而便宜的瑕疵機對能自行維修或拆件的人仍是合理選擇——提供資訊、由人判斷。

**附帶原則**：只標**功能性**瑕疵。外觀使用痕跡是二手常態，全部標註會讓讀者學會忽略徽章。

---

## 4. 下架判定採時間老化，不採「本次未見」

**日期**：2026-08-26　**狀態**：已實作

**背景**：一次執行將 46 筆蝦皮物件標記為下架。

**決定**：`sweep_stale(source, max_age_days)`，預設 14 天未再出現才標記。

**理由**：本專案所有爬蟲取得的都是**取樣視窗**而非完整庫存（PTT 讀近期文章、
蝦皮讀最新約 180 筆）。實測兩次執行的蝦皮商品集合可能完全不相交（35 vs 30，重疊 0），
舊集合並非售出，只是被擠出最新清單。

---

## 5. 行情價差顯示 —— 延後

**日期**：2026-08-27　**狀態**：**延後，條件見下**

**背景**：VFM 分數（如 694.1）單位不明、有假精確，讀者難以判斷「這樣算好嗎」。
曾考慮改為百分位顯示，但百分位是序數，篩選後樣本少時等同排名，資訊量不足。

**決定**：暫不實作，維持現行分數呈現。

**理由**：提案為「推估行情價 = 調整後效能分 ÷ 中位 VFM × 1000」，以金額表達價差。
但實測顯示**模型對不同世代並非中性**：

| 世代 | 筆數 | 組內中位 VFM | vs 全體 |
|---|---|---|---|
| M4 | 24 | 480 | +17% |
| M1 | 31 | 434 | +6% |
| M3 | 25 | 413 | +1% |
| M2 | 36 | 361 | −12% |
| **M5** | **9** | **315** | **−23%** |

若以全體中位數為基準，**每台 M5 都會被說成貴 23%**——那不是洞察，是系統性誤導。
改用同世代中位數可消除偏差，但 M5 僅 9 筆，樣本不足。

進一步以資料反推折舊率：目前的 `每年 ×0.9` 使世代最大偏差 23%，
改為 `×0.95` 可降至 19%，但**剩餘偏差不是折舊率能解決的**——
新機存在「新機溢價」，市場願意為當代機種付超額價格，這是真實市場結構而非模型缺陷。

**重新評估條件**：**每個晶片世代累積 30 筆以上**時重新評估。
屆時同世代中位數才有統計意義，並應同時顯示樣本數，不隱藏不確定性。

---

## 6. Gemini 維持 3.5 Flash Lite

**日期**：2026-08-27　**狀態**：已確認

**決定**：不更換模型，改為主動節流。

**理由**：配額顯示 RPM 22/15（超標）但每日僅 118/500（充裕）——瓶頸是速率不是額度。
根因是 pipeline 以固定 `sleep(1)` 節流，等同 60 RPM。

**否決**：
- 換 Gemini 3.x Flash（非 Lite）：每日僅 20 次，不敷使用。
- 換 Gemma 4 26B：每日 14,400 次，但實測 4 筆有 1 筆抓不出 `M1 Pro Max` 這類複合晶片名稱，
  而無晶片的物件會被過濾器整筆丟棄。**以解析品質換一個用不到的額度並不划算。**

**重新評估**：每日用量逼近 500，或需一次性大批回填時。
屆時可考慮兩段式：Gemma 先跑、`chip` 為空者再交給 Gemini 複驗。

---

## 7. 前後端 VFM 公式不一致

**日期**：2026-08-27　**狀態**：**已修正**

**背景**：同一筆物件在 Dashboard 與後端的分數不同，125 筆中有 59 筆（47%）對不上，
最大差距 55 分。

**原因**：形態加權的判定依據不同——後端 `score_engine` 只看 `series` 字串，
前端 `dashboard` 同時看 `series` 與螢幕尺寸。

**影響**：Discord 警報門檻以**後端**分數判定，使用者看到的是**前端**分數。
實測有 7 筆跨在門檻兩側：顯示為「優秀」卻永不推播，或會推播卻顯示為「普通」。

**決定**：採用前端較細緻的分法（區分 Air 13/15、Pro 13/14/16），並**收斂為單一實作**。
`score_engine` 成為唯一的計算入口：持有 `MacBookSpec` 的呼叫端用 `get_vfm_score`，
持有原始 row 的用 `vfm_from_mapping`，兩者共用 `adjusted_score()`。

一併移除三處會再度漂移的重複定義：Dashboard 自帶的基準表與公式、
`mac_spec` 中無人使用卻與 `ScoringWeights` 數值衝突的權重屬性
（`ram_weight` 1.2 vs 1.25、`ModelSeries.weight` Pro13 1.05 vs 1.0）、
以及三處硬編碼的滑桿預設值——現由 `ScoringWeights` 推導。

FastAPI 的權重 query 參數同步改名為 `form_air13` 等五項。

**驗證**：125 筆全數比對，前後端差異 **0 筆**（修正前 59 筆），
跨越警報門檻的分歧 **0 筆**（修正前 7 筆）。並以瀏覽器實測確認網頁顯示的前五名分數
與後端計算完全相同。

---

## 8. 公開前的資料衛生稽核

**日期**：2026-08-29　**狀態**：已完成

**背景**：專案要從 private 轉公開。公開後任何提交都無法真正收回，因此在轉換前
做一次完整稽核，範圍涵蓋工作目錄與**全部 git 歷史**——後者才是重點，因為
`.gitignore` 只擋未來，擋不住已經進去的東西。

**發現**：現行程式碼乾淨（憑證一律走環境變數，`.env.example` 全為佔位符）。
問題全部在歷史裡，分三類：早期一次 commit 誤帶了 Playwright 的瀏覽器設定檔與
登入 session；提交身分沿用了個人信箱；除錯用的頁面快照裡夾帶第三方個資。

**決定**：以 `git filter-repo` 清除相關路徑並統一提交身分，改用新建的公開 repo
承接乾淨歷史，原 repo 改名保留為 private 封存。

**理由**：force-push 之後 GitHub 仍可能保留無主 commit，且 repo 的 Activity
頁面會留下改寫前的 SHA。**開新 repo 沒有這個問題**——它從來沒有裝過那些物件。
代價只是重設 secrets 與重新部署 Streamlit，換到的是確定性。

**衍生措施**：`CLAUDE.md` 將個資檢查列為強制項目，並明確涵蓋爬蟲抓到的
第三方資料。`.claude/` 這類本機工具設定移出版本控制。

**教訓**：`.gitignore` 是在檔案第一次進 repo **之前**才有用的。一次不小心的
`git add .`，代價是整段歷史。

---

## 9. 規格抽取寧可缺值，不可猜測

**日期**：2026-08-27　**狀態**：已實作（原則）

**背景**：蝦皮改用精簡模式後失去商品描述，RAM／SSD 的抽取覆蓋率由約 80% 掉到 40%。
追查時發現 regex 不只是抓不到，而是**抓錯**：

```
『澄橘』Macbook Pro 13 2022 M2 8C10G/8G/256G
                        ↑ 8核CPU / 10核GPU
被讀成 RAM=10、SSD=8TB
```

8TB 通過「SSD ≥ 1TB」的門檻取得加成，VFM 因此被推高。

**決定**：所有規格抽取在無法確定時回傳 `None`，並以 Apple 實際出貨的規格清單驗證結果。
同理，晶片抽取遇到賣家堆關鍵字寫出的不存在型號（如「M1 Pro Max」）時取**較低**的一階。

**理由**：缺值有後備機制——由 LLM 補、或由計分器套用預設值，且缺值在 UI 上看得出來。
**錯值沒有任何機制能攔截**，它會直接進入 VFM 公式並產生看似可信的分數。
低估只是少一次機會，高估會發出假的撿漏警報。

**衍生**：`src/scripts/repair_specs.py` 可掃描並修正既有的不可能值。
修復時若任一欄位不合法，則**整組重新抽取**——兩個值來自同一次解析，
倖存的那一半不會因為「剛好是合法規格」就值得信任。

---

## 10. 測試不得依賴外部服務

**日期**：2026-08-28　**狀態**：已實作

**背景**：專案 4,161 行、**零測試**。過去發現的每個 bug——GPU 核心數被讀成記憶體、
M5 整個世代被丟棄、前後端分數不一致——都是臨時寫腳本手動查出來的，腳本用完即丟，
同樣的錯再犯一次不會有任何機制攔截。

**決定**：建立 pytest 套件，並訂下硬性約束：**沒有 API 金鑰、沒有資料庫也必須全數通過**。

**理由**：這個約束不只是為了讓 CI 跑得動，它會**逼著純邏輯與 I/O 分離**。
實作時立刻付出代價也立刻看到效果：`llm_parser` 原本在模組載入時就建立 Gemini client，
導致連 `extract_specs_from_text`（純 regex，從不呼叫 API）都無法在無金鑰環境下匯入。
改為延遲建立後，該模組才變得可測。

**測試內容取自實際踩過的坑**，不是憑空設計的案例：
`8C10G/8G/256G`、`外觀無傷無碰撞`、`M1 Pro Max`、`鋁合金類macbook`、標題為 `888` 的佔位商品。

**成效**：撰寫過程中即發現一個未知問題——`ShopeeScraper._listing_from_search_item()`
未自行檢查 L1 價格範圍，僅依賴呼叫端過濾，與 Carousell 的對應實作不一致。
已改為方法自身強制，並將上下界抽為具名常數。

**尚未涵蓋**：需要網路或瀏覽器的部分（爬蟲的實際抓取、LLM 呼叫、Streamlit 畫面）。
畫面測試見階段 5 規劃。

---

## 11. Linter 規則寧可少而準

**日期**：2026-08-28　**狀態**：已實作

**背景**：導入 ruff 時，預設規則在 4,161 行的專案上回報 **145 個問題**。

**決定**：只啟用能抓出 bug 的規則（`F`、`E`、`W`、`I`、`B`、`PLE`、`DTZ`、`RUF`），
關閉純風格項目，並在 `ruff.toml` 中逐條寫明關閉理由。

**理由**：**噪音多到會讓人略過真正的發現，比不裝 linter 更糟。**
關掉的都有具體理由——例如 `BLE001`（捕捉泛用例外）在爬蟲裡是刻意的設計：
單一筆壞資料不得中斷整批；`E701` 針對的 `infer_correct_year` 是寫成 if 鏈的查表，
攤成三倍行數並不會更清楚。

**成效**：導入當下就抓到兩個真問題。

`PLE2510` 指出 `text_extractor.py` 的售價 regex 裡有一個**實體倒退字元**（0x08），
佔據了原本應為 `\b` 的位置：

```
r"([0-9][0-9,]*)([kK])<0x08>"
```

該樣式要求輸入中含倒退字元，永遠不可能匹配——**PTT 上所有寫成「25k」「32k」的售價
一律抽不到**，而那是 PTT 賣家最常見的標價方式。這個字元在編輯器裡不可見，
人工審閱不可能發現。

修正後另加一項測試，掃描全部原始碼中的控制字元，避免同類問題再次潛入。

第二個是 `F821`：修正時區問題的過程中，我在 dashboard 引用了未匯入的 `current_year`，
linter 在執行前就攔下了這個 NameError。

---

## 12. 文件與程式碼的一致性交給 CI 檢查

**日期**：2026-08-28　**狀態**：已實作

**背景**：測試擋得住壞掉的程式碼，擋不住過期的文件。文件錯了不會有任何一項變紅，
下一個人（包含幾個月後的我）會照著三次 commit 前就不再成立的說明去做。

**決定**：寫 `scripts/check_docs.py`，只驗**機械上可驗證**的宣稱，並同時掛在
pytest 與 CI：

1. 程式讀的每個 env 變數都在 `.env.example`，且 `.env.example` 裡的每個鍵都真的有人讀
2. Markdown 的相對連結都指得到檔案
3. CLAUDE.md 表格標為「✅ 已撰寫」的 spec 確實存在（標 ⬜ 卻存在也算不一致）
4. `score-engine/spec.md` 引用的評分常數與 `score_engine.py` 相符

**理由**：第 4 項是主要動機。**讀者是照那張參數表判斷分數合不合理的**，而那些權重
已經調整過一次；表和程式一旦分岔，整份 spec 就不再能當作判斷依據。

不驗散文，也不驗「文件寫得對不對」——那不可能自動化。只驗數字與路徑，
誤報接近零，開發者才不會學會忽略它。

**導入當下抓到的**：

- `.env.example` 的 `API_HOST` / `API_PORT` 標著「P3 後啟用」，但 API 早已實作，
  而且從不讀這兩個值（`docs/operations.md` 用的是 `uvicorn --port 8000`）。
  看起來可設定、實際上動了沒有任何效果，比缺漏更誤導。
- `score-engine/spec.md` 仍寫著「M5 系列目前為外推值」，實際已換成 Geekbench 6 實測。
- 同一份 spec 要求「新增世代必須同時更新 `benchmark_db.py` 與 `dashboard.py` 的
  `_BENCH`」——那兩處在合併 VFM 實作時就已經是同一個 dict 了。照著做的人會去找
  一個不存在的第二張表。

**自我驗證**：另有一項測試餵給檢查器一份被竄改的 spec，確認它會失敗。
不會失敗的檢查器只是裝飾。

---

## 13. 瑕疵判定移到 pipeline，結果隨規格一起存

**日期**：2026-08-28　**狀態**：已實作

**背景**：`find_defects()` 的簽名是 `(*parts)`，設計上就要吃標題、`condition` 與內文。
Dashboard 呼叫時確實傳了 `row.get("body_content")`，Discord 那側則根本沒傳。

但 `get_filtered_deals()` 與 `get_all_parsed_deals()` **都不回傳 `body_content`**——
它們把 `parsed_json` 展開，再外加 `original_title` / `url` / `status` / `source`。
所以 Dashboard 傳進去的永遠是 `None`。

結果：**只寫在內文的功能性故障，對整個系統不存在**。而 PTT 賣家正是把機況寫在內文，
標題只放型號與售價。

**決定**：判定移到 `src/main.py` 存檔前執行——那是全系統唯一還握著 `body` 的位置——
結果存進 `parsed_json` 的 `defects` 欄位。下游一律經 `defects_for(row)` 取用，
沒有該欄位的舊資料退回只看標題與 `condition`。

**否決的方案**：讓 `get_filtered_deals()` 一併回傳 `body_content`。
改一行就好，但 Dashboard 每次重繪都會把所有內文從 Neon 拉一次（每動一次滑桿就重跑），
而它只需要那幾個字串裡的一個布林結論。算一次、存起來，前後端也就不可能再各算各的。

**先前的判斷是錯的**：我一開始說這是前後端不一致。不是。
`main.py` 當時傳的是 `(original_title, condition)`，Dashboard 傳的是
`(title, condition, body_content=None)`——**兩邊算出來的結果一致，而且一致地錯**。
Discord 與網站互相印證，只是都看不到內文。

**測資的教訓**：第一版 e2e 測資把「內文才有的瑕疵」寫成「左下角外殼有明顯凹傷」，
測試失敗。查下去發現失敗的是測資不是程式——外觀磨損本來就刻意不標記
（見本檔 #9 與 `api/spec.md` 4.X.5）。改成「螢幕右下角破裂」才是真的功能性故障。
**紅燈的第一個嫌疑人是測試自己。**

---

## 14. 瀏覽器測試：只驗會默默壞掉的東西

**日期**：2026-08-28　**狀態**：已實作

**背景**：其餘所有測試都在測函式。分數算對了、卻 render 進一個在手機上撐破畫面的
元素，對使用者而言一樣是失敗，而專案裡沒有任何東西會發現。最初的需求就是
「手機端網頁幾乎沒可讀性」。

**決定**：`tests/e2e/` 自建一個固定的 SQLite 測試資料庫（9 筆寫死的物件），
用真正的進入點 `streamlit_app.py` 把服務跑起來，再用 Playwright 驗四件事：

1. 卡片上的分數等於 `score_engine` 算出來的分數
2. 瑕疵徽章出現在標題／`condition`／內文三種來源，且乾淨物件不誤標
3. 320／390／1440px 都不橫向捲動，卡片內文不溢出卡片
4. 零 JS 錯誤、無 traceback 洩漏進頁面

**刻意不驗**設計好不好看——那不可能自動化，也不該由 CI 決定。

**測資是寫死的**，所以每個斷言都有已知的正確答案；分數是拿 `vfm_from_mapping`
現算來比對，不是把畫面上的數字抄下來當期望值——那種測試永遠會通過。

**兩個踩到的坑（都在我自己的測試程式裡）**：

- `subprocess.PIPE` 接了 Streamlit 的 stdout 卻從不讀。**OS pipe 緩衝區滿了以後
  Streamlit 會卡在寫入而停止服務**：前幾次載入正常，之後每一次都 60 秒逾時。
  首次執行 1 失敗 11 錯誤、耗時 11 分 51 秒。改成導向檔案。
- `page` fixture 是 function-scoped，13 個測試各自重載頁面。改成 session-scoped
  共用一個頁面，只有會點擊 widget 的測試另開 context，並用 fixture 在結束時
  把 viewport 復原。**20.18 秒，19 項全過。**

---

## 15. 支援 MacBook Neo（A18 Pro）—— 寫死家族的老問題換個形狀再犯

**日期**：2026-08-28　**狀態**：已實作

**背景**：旋轉拍賣抓到一筆「MacBook Neo 8G/256GB」，售價 20,700。
我一開始判斷它是借用名稱的山寨機。**錯了**——MacBook Neo 是 Apple 2026 年 3 月推出的
$599 機種，也是第一台採用 iPhone 晶片（A18 Pro）的 Mac。

**問題**：`_CHIP_RE` 只比對 `\bM(\d{1,2})...`，A18 Pro 抽不到晶片，
而 `_INVALID_CHIPS` 會把無晶片的物件整筆丟棄。**這台機器對系統不存在。**

這正是本檔 #3（M5 被靜默丟失 9 筆）同一個成因換了形狀：當時是寫死世代清單停在 M4，
這次是寫死家族前綴只認 M。**「不得寫死清單」那條規則的範圍原來比我當初寫的更大。**

**決定**：

- 基準分 `A18 Pro: 8668`（Geekbench 6 多核，與全表同一來源），略高於 M1 的 8,500，
  與發表時的評測結論一致。
- 抽取改為 `\b([MA])(\d{1,2})\s*(PRO|MAX|ULTRA)?\b`。
- **A 系列排序一律低於任何 M 系列**，否則 `A18` 會在數字上壓過 `M5`。
- **裸寫的 `A18` 正規化為 `A18 Pro`**。Apple 只出過這一顆 A 系列 Mac 晶片；
  不正規化會落到 5000 預設分，等於賣家少打兩個字分數就砍半——
  與 #4 那個年份推論 bug（同機種 432 vs 204）同一類的失真。
- 形態歸入 `air13`（13 吋無風扇入門機），否則會落進 `pro13` 這個預設分支。

**碰撞檢查**：Apple 機身型號是 `A` + 4 位數（`A2338`、`A1706`），
`\d{1,2}` 後面接不到 `\b`，不會誤判。已寫成測試。

**教訓**：我把一台沒見過的機型判斷成山寨，是**用「我沒聽過」代替查證**。
硬體會出新型號，而這個專案的價值完全建立在能不能認得它們。

---

## 16. `last_seen` 只能由「真的看到」來更新

**日期**：2026-08-28　**狀態**：已實作

**背景**：查一件無關的事時發現，一次 `--source shopee` 的本機排程，把 PTT 與旋轉拍賣的
資料也改寫了——三個來源的 `last_seen` 都落在同一分鐘。

追下去是 Step 2 解析迴圈：它 `db.get_all_deals()` 掃**全表**、不管 `--source`，
然後用 `db.save_deal()` 寫回結果。而 `save_deal()` 會更新 `last_seen`。

**為什麼嚴重**：`sweep_stale()` 的整個前提是「`last_seen` = 我們最後一次在平台上
看到這筆」。解析迴圈**從不重新抓頁面**，它只讀資料庫裡已存的標題與內文——卻宣稱看到了。

於是任何解析不完整的資料每晚都被蓋成「剛看到」，**永遠不會過期**。
實測 **186 筆中有 121 筆**處於這個狀態。最糟的是那些「缺的欄位本來就不在存下來的文字裡」
的資料：永遠解析不完整 → 永遠需要修復 → 永遠被刷新 → 永遠以「在售中」留在網站上。

**決定**：拆成兩個方法。`save_deal()` 記錄一次目擊、更新 `last_seen`；
新的 `update_parsed()` 只改 `parsed_json`。規則寫進 `database/spec.md`：
**沒有真的去平台上看過，就不准動 `last_seen`。**

---

## 17. 修復條件不得由修復動作自己滿足

**日期**：2026-08-28　**狀態**：已實作

**背景**：解析迴圈的 `needs_fix` 條件之一是 `p_json.get("location") == "未知"`。
而同一個迴圈的結尾是：

```python
if not res_dict.get("location") or res_dict.get("location") == "未知":
    res_dict["location"] = "未知"
```

**修復動作產生了讓它需要修復的那個值。** 這 69 筆資料每晚各花一次 Gemini 呼叫，
永遠不會成功，佔當時每晚 LLM 呼叫的 **57%**。

**決定**：移除該條件。`location` 是卡片上的一個顯示欄位，值不值得為它打一次 LLM
是另一回事，但**用一個保證不會成立的條件去觸發它，只會是純粹的浪費**。

**通則**：任何「需要處理」的判定，都要確認處理動作真的會讓它變成「不需要處理」。
不然就是寫了一個每晚執行的無限迴圈，而且不會有任何東西報錯。

---

## 18. 解析成本必須與新物件數量掛鉤，不是與資料庫總量

**日期**：2026-08-28　**狀態**：已實作

**背景**：`MAX_LLM_CALLS_PER_RUN` 只在蝦皮爬蟲內生效，**解析迴圈完全沒有上限**。
而它掃全表，所以成本隨資料庫**總量**成長：

| 資料庫規模 | 每晚 LLM 呼叫 |
|---|---|
| 186 筆（當時） | 121 |
| 500 筆 | 約 325 |
| **約 770 筆** | **500 —— 撞上 Gemini 免費方案每日上限** |

到那時整條 pipeline 每晚失敗，而症狀會像「Gemini 壞了」，很難追到真正的原因。
**這種 bug 在資料量小的時候完全看不出來，等看得出來的時候已經在生產環境爛掉了。**

**決定**：

1. **解析指紋**。把這次解析所讀的 `title + body` 的雜湊存進 `parsed_json`。
   下次執行時若雜湊沒變，代表要問的是**同一個問題**，跳過。
   物件被重新抓取而文字改變時，雜湊跟著變，自動恢復重試。
2. **`MAX_REPAIR_CALLS_PER_RUN`（預設 50）** 作為兜底上限。
3. 解析迴圈只處理**這次有跑的來源**（`--skip-scrape` 除外，那個模式的用途就是重解析全部）。

**成效**：每晚 121 次 → 第一晚 50 次 → 之後 0 次（除非有新物件或文字有變動）。

**否決的方案**：單純加上限、每晚輪流修一批。實作簡單，但那只是把浪費攤平到更多天，
沒有解決「同樣的問題問一輩子」這件事。

---

## 19. 可執行的腳本，不是要人照著重打的註解

**日期**：2026-08-28　**狀態**：已實作

**背景**：本機排程的註冊指令寫在 `run_local_shopee.ps1` 的檔頭註解裡，
`docs/setup.md` 則寫「依檔頭註解註冊工作排程器」。也就是說換電腦的程序是
**讀一段註解、手動重打一次**。

檢查時發現註解和實際註冊的內容**已經不一樣了**——註解寫 `push to Neon`，
實際是 `push results to Neon`。差一個字沒有影響，但它證明了這種做法必然漂移，
而且漂移不會有任何東西報錯。真正的代價是：換電腦時照著註解打，會得到一個舊版本的設定
（少了 `-WindowStyle Hidden`，也不會有之後決定的來源清單）。

**決定**：改成 `scripts/install_schedule.ps1`——**這個檔案就是排程的定義**，不是描述。
要改時間或來源就改參數重跑，它會就地覆寫。同時在註冊前檢查 venv、`.env`、
蝦皮 session 是否就緒：一個每晚安靜失敗的排程，沒有人會發現，因為它成功時本來就是安靜的。

`docs/setup.md` 補上「換電腦」章節，明確列出**四樣不在 repo 裡的東西**
（venv、`.env`、`shopee_state.json`、Windows 工作排程）以及各自為什麼不在。
資料本身不用搬——它在 Neon。

**通則**：任何需要人照著重打才能重現的程序，都應該視為尚未寫下來。

---

## 20. 旋轉拍賣移到本機執行

**日期**：2026-08-28　**狀態**：已實作

**背景**：`carousell.py` 的模組註解原本宣稱：純 HTTP、伺服器端渲染、不需瀏覽器，
所以「可在 GitHub runner 上原樣執行，不像蝦皮」。2026-08-28 的排程以
`403 Forbidden` 失敗，證明那句話是錯的。

**量測**（`src/scripts/probe_carousell.py`，手動觸發的 workflow）：

| | GitHub runner | 住宅 IP |
|---|---|---|
| 5 種標頭（含現行、最新 Chrome、完整瀏覽器標頭、誠實爬蟲 UA、無標頭） | **5/5 403** | 5/5 200 |
| 5 個路徑（sitemap、sitemap 索引、robots.txt、首頁、商品頁） | **4/5 403** | 5/5 200 |

唯一通過的是 `robots.txt`。**這是 CDN bot 管理的典型特徵**——刻意放行 robots.txt
讓爬蟲讀得到規則，其餘一律擋。所以是針對資料中心 IP 的政策，不是路徑問題，
也不是渲染方式的問題。三次獨立的 runner 執行結果一致。

**決定**：CI 只跑 PTT；旋轉拍賣與蝦皮一起在本機排程執行，旋轉排在前面
（純 HTTP、約一分鐘，先跑完就不受蝦皮瀏覽器路徑卡住的影響——排程上限是一小時）。

**為什麼不留在 CI 讓它失敗就好**：那會每晚發一則「旋轉拍賣：爬取失敗」，
而那是一個**永遠不會自己好的已知狀況**。天天喊狼來了，真的失敗時也會被一起忽略——
與瑕疵徽章、linter 規則篩選是同一個原則。

**沒有測到的部分**（誠實記下）：付費住宅代理沒測，但它一定可行，這個量約 $2/月。
Carousell 的 `api-service` 端點、其他網域、IPv6 都沒測。所以精確的說法是
**「在不花錢的前提下只能本機」**，不是「技術上做不到」。

**保留量測工具**：WAF 規則會變。probe 腳本與 workflow 都留著，
日後想確認封鎖是否解除，執行一次就有答案，不必重新推理。

---

## 21. 429 是兩種問題共用一個狀態碼

**日期**：2026-08-28　**狀態**：已實作

**背景**：套用完本機排程後手動觸發一次，Gemini 開始回 429。`llm_parser.py` 的重試
邏輯註解直接寫著「A 429 here is the per-minute quota」，於是每次都睡 65 秒再試。

但那次的 429 是**每日**額度：

```
quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier
limit: 500
```

**RPD 要等到太平洋時間午夜才重置，睡 65 秒毫無意義。** 實測結果：那次執行 20 分鐘裡
出現 24 次 429、睡掉 18 分鐘，只處理到第 19 筆（共 160 筆），照這個速度會在排程的
一小時上限被砍掉——**Step 3 到 7（評分、警報、下架判定、heartbeat）一個都跑不到**。

**決定**：判讀錯誤內的 `quotaId` 而非訊息文字（文字會改版，quotaId 不會）。
每分鐘額度維持原本的等待重試；每日額度拋出 `GeminiDailyQuotaExhausted`，
解析階段立刻中止，pipeline 帶著已解析的資料繼續走完剩下的步驟。

**額度為什麼今天就用完**：今早 09:41 那次排程跑的是**修正前的舊程式碼**——
無上限地重新解析全表 121 筆。加上蝦皮爬蟲本身的 LLM 呼叫（**共用同一個 500 次額度**，
這點我在 #18 的估算裡漏掉了），所以我當時估的「約 770 筆才會撞牆」太樂觀，
實際上當天就撞到了。

**Discord 的呈現**：額度用盡**不算爬取失敗**。所有來源都可能抓得好好的，只是沒解析完；
報成「爬取失敗」會讓人去查爬蟲，查錯方向。改成獨立一行的 🪫 提示，
而來源真的失敗時仍然優先顯示 ⚠️。

**順帶補上 #18 的漏洞**：被晶片過濾丟棄的物件先前不會記錄解析指紋，
所以每次執行都會重新問一次同樣的問題——正是這個預算機制要防的事。
現在會記錄，但**只針對已經有 parsed_json 的資料**：替從未解析成功的資料寫入一個
只含指紋的 parsed_json，會讓它出現在 Step 3 的評分輸入裡（該處只檢查 parsed_json 非空）。
從未解析成功的資料，一天重試一次是合理的。

---

## 22. 列表改版：結果列，機型當大字

**日期**：2026-08-28　**狀態**：已實作

**背景**：最初的三個問題裡，UI 是唯一沒動過的——「表格固定格式、有點醜，手機端幾乎沒可讀性」。
先做完邏輯與測試才動它，是因為**改 UI 需要安全網**：現在有 e2e 測試守著
320／390／1440px 不橫向溢出、卡片分數與後端一致、零 JS 錯誤，所以可以放手改。

**決定與理由**：

- **卡片改為髮絲線分隔的資料列。** 舊版每個欄位權重都一樣，眼睛不知道先看哪個。
  航班與比價網站的做法是讓價格對齊成一欄，眼睛直接往下掃。
- **機型當大字，賣家原標題退為灰色副標。** 賣場標題把型號埋在店名、優惠標語與貨號裡
  （`『澄橘』Macbook Air 15 2025 M4 10C10G/16G/256G 午夜 瑕疵機《二手》A87078`）。
  這不只是排版——**系統開始為自己的解析結果背書**，顯示的是結論而不是原文。
  原標題保留，因為它是唯一能核對上面那行的依據。
- **分數單位標在清單上方一次，不是每列。** 「CP」沒有回答任何事。這個數字有真實單位：
  公式是 `效能分 × 折舊 × 加成 ÷ 售價 × 1000`，所以就是**每千元買到的效能分**，可以驗算。
  標頭同時放中位數——**750 要先知道中間值是 415 才有意義**。
  這與 #6 否決百分位並不衝突：中位數是當下的事實，不是排名宣稱。
- **淺色。** 使用者判斷更接近蘋果調性。
- **Inter + Noto Sans TC，數字用 `tabular-nums`，不用等寬字。**
  等寬字是開發工具的語言。Apple 從不用它排價格，而是用 SF 搭配等寬數字——
  **這是「工具感」與「產品感」的分界，比選哪個字體影響大。**

**被否決的**：

- **留白版（大量空白、細體大字）**：「比較美觀但資訊閱讀不容易」。
  對一個每天回來掃一眼的工具，可讀性優先。
- **首頁 hero 大圖／slogan**：那是比價網站「首頁」的做法，而 mac-valuer 只有結果頁。
  放上去會吃掉手機第一屏——正是最初抱怨的那個問題。
- **SF Pro／PingFang TC**：Apple 的字體授權只允許用於 Apple 平台的 App，
  放在公開網站當 webfont 是違反授權的。Inter 是最接近的免費替代。

**自己犯的同一個錯**：第一版改完，手機上標題與統計列吃掉整個第一屏，
**第一筆物件在摺線以下**——就是我拿來否決 hero 的那個理由。
已修（標題限高、統計改兩欄、價格區間排到最後）。

---

## 23. 規則改變時，已存的資料不會自己跟上

**日期**：2026-08-28　**狀態**：已實作

**背景**：修好 Intel Core m5 被誤判為 Apple M5 之後，**那台 2016 年的機器仍然掛在網站榜首**，
分數 1060。修正只作用於新的解析；資料庫裡既有的 `chip=M5` 不會因為規則變了就自動重算，
而它的 `parsed_json` 是完整的，解析迴圈也不會回頭碰它。

**決定**：寫 `src/scripts/revalidate_chips.py`，以**現行規則**重新檢驗每一筆已存的晶片。
不通過的清掉 `parsed_json` 而非刪除整列——物件確實存在，只是超出範圍，
而「沒有解析結果」本來就是 pipeline 表示這件事的方式（所有讀取路徑都要求 `parsed_json` 非空）。

**通則**：**判定規則改變時，要問「已經存下來的資料怎麼辦」。**
只修程式碼會留下一批依舊照舊規則產生的資料，而且它們通常正是當初讓你發現問題的那幾筆。

---

## 24. 網站更名：不做的事就不要寫在標題上

**日期**：2026-08-28　**狀態**：已實作

**背景**：頁面標題是「二手 MacBook 估價」，錯誤畫面則是「二手 MacBook 智慧估價系統」。

**估價指的是「給我一台機器，我告訴你值多少」——這個系統沒有這個功能，也沒打算做。**
它做的是相反方向：把三個平台的在售物件抓回來，用每千元買到多少效能來排序。
（「智慧」則純粹是贅字。）

**決定**：改為「**Mac 好價雷達**」。

- **雷達**＝持續掃描。每天 02:30 自動執行、Discord 推播撿漏警報，這就是雷達的行為。
- **好價**＝它衡量的東西。比「價值」口語、比「撿漏」中性，且不暗示估價。
- **Mac 而非 MacBook**——這是最要緊的一項。日後若納入 Mac mini 等機種，
  現在改名幾乎沒有成本；等到有資料才改，就要動網址、文件與所有人的記憶。
  repo 名 `mac-valuer` 本來就是 Mac 尺度，不必動。

  **但名字便宜，引擎不便宜**：`form_factor_key()` 目前以螢幕尺寸分
  air13／pro14／pro16，而 Mac mini 沒有螢幕。真要收非筆電機種，
  形態加成得重新設計——那是另一件工程，不在這次範圍。

被否決的：「CP 值排行」（太直白，把畫面上的欄位名搬上來當標題）、
「觀測站」（沒說觀測什麼）、「行情板」（「行情」會讓人期待歷史價格走勢，而行情價差還沒做）、
「價值雷達」（拗口）、「MacFinder」（Finder 是 macOS 的檔案管理程式，在 Mac 產品上用會混淆）、
「MacWorth」（好唸，但 worth 在英文偏「值多少錢」——換成英文的同一個估價問題；
純英文名也讓中文使用者第一眼讀不到任何資訊）。

標題上方小字改為列出實際來源與更新頻率，由 `SOURCE_LABELS` 推導——具體的說明交給小字，
標題只負責身分。

**通則**：**產品名稱是一種承諾。** 寫上一個沒有實作的能力，使用者第一次嘗試就會發現，
而那比名字平淡難挽回得多。這條規則已寫進 `api/spec.md`。

---

## 25. 色帶基準取自全站，而且要說出來

**日期**：2026-08-28　**狀態**：已實作

**背景**：本檔 #6 否決了「用百分位呈現分數」，理由是樣本少時百分位等同排名——
篩選剩四筆，第二名就是 25%，沒有參考價值。

但色帶（划算／普通／偏貴）本來就是用 p75／p50 切的。**差別在於它取自
`all_available`（全部在售物件）而非篩選後的清單**，所以篩選不會改變判定標準。
這個設計一直是對的——**但畫面上沒說**，讀者無從判斷。

**決定**：圖例加上「全站基準」前綴，清單標頭寫「全站中位數」。
一個沒有說明依據的色帶，是沒有人會信的色帶。

**加了測試釘住這個性質**：`test_filtering_does_not_move_the_standard` 會勾選
「隱藏瑕疵品」，然後斷言圖例與標頭的數字完全沒變。
蓄意把閾值改成看篩選後的清單時，中位數從 415 掉到 370，測試如預期失敗。

**這不是可有可無的測試。** 那個性質完全看不出來——它只是幾行算 `np.percentile`
的程式碼取用了哪一個變數，改錯了畫面照樣正常顯示，只有數字悄悄變得沒有意義。

---

## 26. 說明區只做一件事：讓分數可被驗算

**日期**：2026-08-28　**狀態**：已實作

**背景**：「VFM 分數構成」展開後 924px，同時做四件事——列公式、列規則表、
畫目前權重長條圖、列晶片基準分。**前三者講的是同一件事**，
而側邊欄的滑桿是第四次講同一件事。

實際毛病：

- 長條圖的 `plot_bgcolor` 寫死 `#0e1117`，換成淺色主題後就是頁面中間一塊黑。
- 公式用 `st.code`，尾端 `÷ 售價 × 1000` 被裁掉——**公式最重要的部分看不到**。
- 1:1 分欄讓左欄過窄，表頭全被硬拆：「晶片基／準」「年份折／舊」「形態加／成」。
- 基準分表按分數排序又固定高度 210px，可見的永遠是 M5 Max／M5 Pro／M2 Ultra／
  M4 Max／M1 Ultra——**五顆幾乎沒人在買的晶片**，而 M1～M4 要捲動。
- **規則表把基準分手抄成散文**（`M1=8500 / M2=10000 / M3=11500 / M4=14500`），
  已經跟不上 `CHIP_BENCHMARKS`：M5 與 A18 Pro 都不在裡面。
  這是本專案修過多次的同類漂移，而 `check_docs.py` 抓不到它——**它在程式碼裡，不在文件裡**。

**決定**：只留下能讓人驗算的東西。

1. **真實物件的逐步算式**，且每一步標明為何套用該乘數
   （`× RAM 加成　16 GB　≥ 16 GB`）。**這樣算式本身就承載了規則**，
   刪掉規則表沒有損失任何資訊。它用的是當前滑桿設定，所以調權重這裡會跟著變。
2. **刪掉長條圖**。權重就在側邊欄滑桿上、你在那裡調它；畫一張圖重複同樣的數字
   沒有增加資訊。連帶移除 plotly 相依。
3. **基準分改為依世代排列的完整網格**，全部 19 顆一次看完，不必捲動。

924px → 673px。

**通則**：**同一件事在畫面上出現第三次，通常代表沒有一次做對。**
真正的解法是找出哪一種呈現能取代其他兩種，而不是把三種都調漂亮。

---

## 27. 一個 bug 的兩層：壞掉的，和慢到像壞掉的

**日期**：2026-08-28　**狀態**：已實作

**回報**：「按下一頁，看到 loading 模糊畫面，但還是停留在原本頁面。」

查下去是兩個獨立的問題疊在一起，而**先找到的那個不是主要成因**。

### 一、下一頁真的沒有作用

頁碼下拉選單同時給了 `key="pg_select"` 和 `index=current_page - 1`。
**Streamlit 的 widget 一旦有 key，session_state 裡存的值會蓋過 `index`。** 於是：

```
按下一頁 → page_num = 2 → rerun
下拉選單還原成存著的 1（index 被忽略）
1 != 2 → 把 page_num 設回 1 → 再 rerun
```

**按鈕的動作被旁邊的元件抵銷了**，而且跑兩次 rerun——所以會看到兩次模糊。

修法：讓下拉選單自己擁有那個狀態（`key="page_num"`），按鈕改用 `on_click` 回呼
在 rerun 前寫入。單一來源，沒有第二個元件能覆寫它。

### 二、每次互動 4.5 秒

實測每次 rerun 對 Neon 查四次，每趟約 1 秒：

| 查詢 | 耗時 |
|---|---|
| `get_filtered_deals`（篩選後） | 1.58s |
| `get_filtered_deals`（未篩選基準） | 0.91s |
| `get_new_count` | 1.00s |
| `get_last_seen` | 1.02s |

**翻頁不改變任何篩選條件，卻付了全額。** 加上 `@st.cache_data(ttl=300)` 後
**5.0 秒 → 0.3 秒**。資料一天只在排程時變一次，五分鐘的快取遠比那短。

### 教訓

我一開始只看程式碼就斷定是 `key`/`index`，測完卻發現舊版的「上一頁」會改變清單——
證據矛盾。**是加了快取、把延遲拿掉之後，功能性 bug 才乾淨地顯露出來**
（舊控制項按兩次下一頁後，上一頁仍是 disabled）。

**慢會偽裝成壞。** 在延遲被排除之前，任何互動的行為觀察都不可信。

兩個都加了瀏覽器測試：翻頁必須換一組物件、回上一頁必須回到原本那組、
且反覆翻頁要能走遍全部測資。

---

## 28. 通報者不能活在被通報的東西裡面

**日期**：2026-08-29　**狀態**：已實作

**背景**：2026-08-29 10:02:46 排程啟動，10:02:51 死亡，全程五秒，**Discord 一個字都沒有**。

事件記錄檔（`Microsoft-Windows-CodeIntegrity/Operational`）：

```
Event 3077 / 3033
process …\Python313\python.exe attempted to load
  …\venv\Lib\site-packages\pandas\_libs\tslibs\timezones.cp313-win_amd64.pyd
did not meet the Enterprise signing level requirements
  (Policy ID:{0283ac0f-fff1-49ae-ada1-8a933130cad6})
```

該 Policy ID 是**智慧型應用程式控制（Smart App Control）**，本機為強制執行
（`VerifiedAndReputablePolicyState = 1`）。它封鎖未簽章且雲端信譽查不到的執行檔，
而 pandas 的 `.pyd` 全部未簽章。

**這不是版本問題**：pandas 3.0.5 於 8/25 安裝，8/26、8/27、8/28 三天都正常執行。
過去十天的全部 Code Integrity 封鎖事件中，**與 python 有關的只有這一次**
（同一秒的 3077 + 3033 兩筆）。事後手動 `import pandas` 亦正常。
結論是**間歇性的信譽查詢失敗**，不是永久封鎖。

而通知之所以完全沒發出，是三層都沒接住：

| 層 | 為什麼沒接住 |
|---|---|
| `src/main.py:9` `import pandas` | 死在這裡。通知器在**第 15 行**才 import，模組連載入都沒有 |
| `send_heartbeat()` | 在 Step 7，`run_valuation_pipeline()` 的最後 |
| `__main__` | 裸呼叫，沒有 try/except |
| `run_local_scrape.ps1` | 把 `exit code 1` 寫進 log 然後結束，**沒有任何人讀那個結束碼** |

**決定**：失敗通知改由 PowerShell 包裝層發出，並加上一次條件式重試。

1. **通知從 python 之外送出**。`Send-FailureNotice` 自行讀 `.env` 取得 webhook，
   附上 log 尾端 12 行。已實測發送成功。
2. **快速失敗才重試**（`$FastFailSeconds = 300`）。300 秒內死亡代表根本沒啟動——
   DLL 被擋、session 檔不見、資料庫連不上——重試幾乎免費；二十分鐘後才死代表已經
   做了很多事，而排程有一小時上限（`install_schedule.ps1`），重試可能被砍在寫入中途。
3. **`__main__` 補 try/except**，涵蓋 pipeline 內部的致命錯誤。
   `_heartbeat_content` 新增 `fatal` 分支：中止的執行**不印「觸發警報：0 筆」**——
   那句話是真的，但讀起來像平靜的一晚。

**理由**：**負責通報的東西不能與被通報的東西同生共死。** 任何由 python 發出的通知，
在 python 起不來時一定不存在。這是 #21（爬蟲壞掉被顯示成「0 筆」）的更高一層：
當時的問題是訊息內容指向錯誤的地方，這次是**根本沒有訊息**。
**沉默沒有形狀——沒有人能對一則從未抵達的訊息設警報。**

**否決**：

- **關掉 Smart App Control**。它是全開或全關，且關閉後需重灌 Windows 才能再啟用；
  而且它擋的確實是未簽章的二進位檔，關掉是拿整台機器的防護換一個排程的穩定性。
- **只在 python 端補 try/except**。這是三層裡最容易做的一層，也是唯一涵蓋不了
  今天這種失敗的一層。單獨做它會製造「已經修好了」的錯覺。

**重新評估**：若 SAC 再擋到其他檔案、或出現重試也救不回的案例，
300 秒的快速失敗門檻要重新檢視。

---

## 29. 共用步驟不能只用被移除者的理由來刪

**日期**：2026-08-29　**狀態**：已實作

**背景**：Discord 回報 CI 的 PTT 爬取失敗：

```
BrowserType.launch: Executable doesn't exist at
  /home/runner/.cache/ms-playwright/chromium_headless_shell-1223/…
```

`ba2d143`（2026-08-26，「schedule Shopee locally, keep CI to PTT only」）在改寫
workflow 時刪掉了 `playwright install chromium --with-deps`。該 commit 自己寫下的
理由是：

> The browser-based Shopee scraper cannot work on a GitHub runner … **and
> camoufox has no browser installed.**

**這個理由屬於當時被移出 CI 的蝦皮。** 但 PTT——那次被**留下來**的來源——
每一篇文章都在開 Chromium（`src/scrapers/ptt.py`，舊版第 96 行）。
刪掉共用步驟的正當性在被移走的那一半成立，在留下的那一半不成立，而沒有人去對。

**影響**：自 8/26 起連續三晚，CI 一筆資料都沒進來。資料庫中 PTT 的最後更新時間為
`2026-08-28 01:59 UTC`，對應的是一次本機／手動執行，不是 CI。

**決定**：PTT 改為純 HTTP，workflow **不裝瀏覽器**。

**理由**：先量再改——`_main_content_text()` 與 Playwright 的 `inner_text()`
在**六篇實際文章**上比對，正規化空白後**完全相同**：

| 文章 | Playwright | HTTP | 相同 |
|---|---|---|---|
| M.1787975683.A.C67 | 748 | 737 | ✅ |
| M.1787975066.A.FBF | 312 | 310 | ✅ |
| M.1787972940.A.12F | 235 | 234 | ✅ |
| M.1787971957.A.403 | 223 | 223 | ✅ |
| M.1787971934.A.255 | 445 | 444 | ✅ |
| M.1787971128.A.36C | 318 | 317 | ✅ |

長度差異全部來自空白正規化。**瀏覽器沒有換到任何東西**，卻讓 CI 多了一個會被忘記的
安裝步驟。**不存在的相依不會被忘記**——這比「記得裝」可靠。

**附帶結論一**：**警報送達 ≠ 有人處理。** 這三晚 Discord 每晚都正確地送出 ⛔，
而且訊息直接指向 Playwright。通報機制運作正常，迴圈卻沒有被關掉。
警報的價值不在送出的那一刻結算。

**附帶結論二**：spec 也漂移了。`scraper/spec.md` 原本**完全沒有 PTT 章節**，
卻在旋轉拍賣的段落寫「純 HTTP，因此可在 GitHub Actions 上執行，與 PTT 同級穩定」
——那句話當時對 PTT 是錯的（PTT 在開瀏覽器），對旋轉拍賣也已經是錯的
（8/28 實測 runner 403、住宅 IP 200，見 #20）。**沒有章節的模組不會被檢查。**

**否決**：**只把 `playwright install` 加回去**。這能讓當晚恢復，但留下的仍是一個
「必須被記得」的相依，而它已經證明了自己會被忘記；下一次有人基於另一半的理由再刪一次，
同樣會壞。止血時確實先加了回去，做完 PTT 改寫後再移除——因為留著它就會配上一段
不再成立的註解，而註解漂移正是本專案反覆在修的東西。

**重新評估**：若將來有需要瀏覽器的來源回到 CI，安裝步驟必須與它同時加回；
workflow 中該位置的註解已寫明這件事。

---

## 30. CI 的依賴清單不再手寫

**日期**：2026-08-29　**狀態**：已實作

**背景**：`pytest` job 長期在 collection 階段就失敗，一個測試都沒跑到。安裝步驟手列了
八個套件，實際需要十二個——測試 import `src.scrapers`，其 `__init__` 會載入全部三個
爬蟲，因此需要 `feedparser`（PTT）與 `playwright`（蝦皮）；`src.main` 需要 `tabulate`；
解析器需要 `google-genai`。

這個失敗一直沒被注意到，是因為 browser job 是綠的，首頁只顯示
「1 failing and 1 successful」——**一半綠的比全紅的更容易被略過。**

**決定**：改為 `pip install pytest ruff -r requirements.txt`。

**理由**：手寫的依賴清單，是一份「已經存在於別處的事實」的副本。本專案已經修過四次
同類問題——效能基準分抄進說明文字、篩選上限抄進 Dashboard、晶片世代抄進爬蟲，
這是第四個。副本不會自己跟上，而且它壞掉的時候沒有人會發現。

**否決的方案**：把缺的四個套件補進清單。快，但下次新增依賴又會漂，
而且下次同樣不會有人發現——這正是它能壞這麼久的原因。

**修正的錯誤前提**：原本不用 requirements.txt 的理由寫在註解裡：「playwright 和
camoufox 會抓瀏覽器二進位檔」。實測 `pip install` 並不會，抓的是 `playwright install`。
證據就在同一個檔案裡：browser job 額外寫了 `python -m playwright install
--with-deps chromium`，若 pip 就會抓，那行不需要存在。

**代價**：實測完整安裝 27 秒。`pytest` job 的設計目的是「兩分鐘內回報邏輯迴歸」，
這個增量在預算內。

**重新評估的條件**：若安裝時間成長到讓 job 超過兩分鐘，再考慮拆一份
`requirements-test.txt`，並用 `check_docs.py` 機械驗證它涵蓋所有 import——
清單可以存在，但不能靠人記得同步。

---

## 31. 公開改用新 repo，而不是改寫舊 repo 後強推

**日期**：2026-08-29　**狀態**：已完成

**背景**：稽核（#8）發現歷史裡有登入 session 與個資，必須在公開前清除。
手段有二：在原 repo 上 `git filter-repo` 改寫後 force-push，
或把乾淨歷史推到一個新建的 repo。

**決定**：新建 repo 承接乾淨歷史，原 repo 改名為 `mac-valuer-archive` 並維持 private。

**理由**：force-push 之後 GitHub 仍會保留一段時間的無主 commit，理論上可經完整 SHA 存取。
更關鍵的是 **repo 的 Activity 頁面會留下改寫前後的 SHA**，而那頁會隨 repo 一起公開——
等於把鑰匙留在門口。官方對「移除敏感資料」的建議因此包含聯絡 GitHub Support 清除快取，
那是一個沒有明確完成時點、也無法自行驗證的步驟。

新 repo 沒有這個問題：**它從來沒有裝過那些物件**，所以不需要相信任何清除動作有生效，
可以直接驗證。

**否決的方案**：

- **在原 repo force-push**：省下重設 secrets 與重新部署，但把「清乾淨了嗎」
  變成一個要靠 GitHub 內部行為與 Support 回應才能回答的問題。
- **刪除原 repo 再同名重建**：一樣乾淨，但刪除不可逆，而且**同名重建會放棄
  GitHub 90 天內還原刪除 repo 的機制**。改名封存有同樣效果，且隨時能退回。

**代價**：重設 4 個 Actions secret、重新部署 Streamlit、重填 About，實測約 20 分鐘。
原 repo 改名後名稱立即釋出，新 repo 沿用原名，因此程式碼中三處寫死的 repo 網址不必修改。

**驗證**：以不帶任何憑證的 `git clone` 取得公開 repo，掃描全物件庫，個資命中 0。
**驗證必須從外部做**——在本機或已登入的環境檢查，看到的不是訪客看到的東西。

**重新評估的條件**：`mac-valuer-archive` 仍含完整洩漏歷史，只是 private。
它同時是還原點與未爆彈。新 repo 穩定運作後應刪除，不要無限期保留。

---

## 32. Streamlit app 的公開設定，與 repo 的公開設定是兩回事

**日期**：2026-08-29　**狀態**：已修正

**背景**：repo 轉公開後，`mac-valuer.streamlit.app` 對未登入訪客仍回 303，
重導到 `share.streamlit.io/-/auth/app`。但用無痕視窗測試看起來完全正常。

**原因**：無痕視窗內的分頁**共用 cookie**。修 app 時在同一個無痕視窗開過
`share.streamlit.io`，因此帶著登入 cookie；auth 那一跳靜靜通過又轉回來，
畫面與正常無異。真正的匿名訪客會停在登入頁。

**決定**：Streamlit Community Cloud 的 app 分享設定必須單獨改為 public。
repo 公開不會連帶讓 app 公開。

**驗證方法**：用 `curl` 而不是瀏覽器。但 curl 預設不保存 cookie，
Streamlit 的 session 交握會因此變成無窮重導，看起來像壞掉——**必須帶 cookie jar**：

```bash
curl -s -L -c cj.txt -b cj.txt -o /dev/null      -w '%{http_code}
' https://mac-valuer.streamlit.app
```

`200` 才算通過。這個坑踩了兩次：先用一般瀏覽器誤判成好的，
再用無 cookie 的 curl 誤判成壞的。

**教訓**：驗證「訪客看得到什麼」時，**測試環境本身的狀態就是變數**。
`docs/operations.md` 已記過一次同類實測（用未登入瀏覽器確認訪客看得到哪些元件），
這次是同一個陷阱換了個形狀。

---

## 33. Postgres 連線要設逾時，不能信任 OS 預設值

**日期**：2026-09-11　**狀態**：已修正

**背景**：2026-09-11 排程執行時，整條 pipeline 因連不上 Neon Postgres 而中斷，
`logs/scrape_2026-09-11.log` 顯示三個解析出來的 IP 全部 TCP 逾時，光是第一次嘗試
就卡了約 74 秒，`run_local_scrape.ps1` 重試一次後仍失敗，總共卡了約 3.5 分鐘才回報
Discord。`DBManager.__init__` 建立 `create_engine` 時沒有設定 `connect_timeout`，
psycopg2 因此使用作業系統的預設逾時值（在 Windows 上遠長於合理範圍），而 Neon 端點
會解析出多個 IP，每個都要各自等到逾時才輪到下一個。

**決定**：`create_engine` 對 Postgres 連線加上 `connect_args={"connect_timeout": 10}`
（sqlite 路徑不受影響，因為該參數不適用）。

**理由**：失敗要快，既有的「重試一次」機制才真正有意義——把整個重試預算耗在等待
單次連線逾時上，等同沒有重試。10 秒對一次即時連線嘗試已經足夠寬鬆，不會誤殺短暫的
網路抖動。

**否決的方案**：在應用層加更多重試次數或 exponential backoff——治標不治本，真正的
問題是單次嘗試等太久，不是嘗試次數不夠。

**驗證**：實測對一個刻意不存在的位址（`10.255.255.1`）建立連線，加上
`connect_timeout=10` 後在 **10.1 秒**內拋出 `OperationalError`（未設定前無上限，
本次事故中單次嘗試卡了約 74 秒）。`pytest tests/ -q --ignore=tests/e2e` 全過。

**重新評估的條件**：若之後改用連線池服務（pgbouncer、Neon pooled connection），
需重新檢視 10 秒是否仍是合適的值——池化連線的逾時語意不同於直連。

---

## 34. CI 探測腳本不再把蝦皮 session 打包成公開 artifact

**日期**：2026-09-11　**狀態**：已修正

**背景**：作品集體檢時發現 `.github/workflows/shopee-ci-test.yml` 會把探測完成後的
`shopee_state.json`（含蝦皮登入 cookie）以 `actions/upload-artifact` 上傳，保留期
1 天。這支 workflow 本身是手動觸發、有詳細註解說明「這是活的登入憑證」的刻意技術驗證
手段，但公開 repo 上任何能看到該次 run 的人都能在保留期內下載這個 artifact，等於把
一把活的鑰匙放在公開的門口——與 #31 記錄的教訓（清乾淨的驗證必須從外部做，且不留下
可被存取的殘餘）是同一類風險。

**決定**：移除 `upload-artifact` 步驟，改為新增 `src/scripts/session_fingerprint.py`，
在還原 session 後與探測跑完後各印一次「檔案大小 + SHA256 前 12 碼 + cookie 名稱與
到期時間」，在 log 裡人工比對前後兩次的雜湊即可判斷蝦皮是否 rotate 或作廢了 cookie，
不需要下載任何檔案。

**理由**：這支 workflow 存在的目的只有一個——回答「蝦皮 session 是否還有效、是否被
這次探測動作改變」，雜湊比對能回答同一個問題，且指紋值本身即使外流也無法還原成可用
的登入憑證。

**否決的方案**：
- **縮短保留期**：1 天已經是 `actions/upload-artifact` 支援的最短值，無法再縮短。
- **限制 artifact 存取權限**：GitHub Actions artifact 的存取權限跟著 repo 可見度走，
  公開 repo 沒有「只有特定人能下載」這個選項，除非把整個 repo 改回 private——
  這違背了 #31/#32 讓專案公開的既有決定。

**驗證**：本機以偽造的 state 檔案跑過 `session_fingerprint.py`，確認輸出只有雜湊值、
cookie 名稱與到期時間，不含 `value` 欄位（實際的 cookie 內容）；對不存在的路徑呼叫
會印出「無 session」而非拋出例外，不會讓 workflow 因檔案缺失而失敗。

---

## 35. Streamlit 休眠的對策要真的打開瀏覽器，curl 只是看起來有用

**日期**：2026-09-11　**狀態**：已實作（遷移評估延後）

**背景**：`mac-valuer.streamlit.app` 部署在 Streamlit Community Cloud，
`docs/operations.md` 記載的額度是「約 12 小時無流量即休眠」。休眠後下一位訪客看到的
是喚醒中的畫面而非儀表板——對個人工具影響有限，但對一個作品集連結，陌生訪客只會點開
一次，遇到休眠畫面等於直接流失。

**第一版決定（同日內被推翻）**：新增 `.github/workflows/keep-alive.yml`，每 6 小時
對 app URL 發一次帶 cookie jar 的 `curl` 請求。**這個版本沒有用**——curl 只驗證了
「HTTP 回應 200」，而 Streamlit Cloud 對休眠中的 app 一樣回 200，回應內容卻是靜態的
「Zzzz…」喚醒畫面，不是真正的 app。真正讓 Streamlit 判定「有人在用」、以及能讓休眠
app 醒過來的動作，都要靠瀏覽器載入頁面後執行的 JS 開啟 websocket、以及使用者點擊
「Yes, get this app back up!」按鈕——curl 兩件事都做不到。

**修正後的決定**：改用 `src/scripts/wake_streamlit.py`，以 headless Chromium
（Playwright）實際載入頁面。實測發現 app 內容渲染在一個同源 iframe 裡（觀察到的路徑
形如 `/~/+/`，內部再巢狀一層 frame），且休眠畫面與正常畫面可能出現在不同 frame——
兩者的確切位置都不是文件化的行為，因此腳本改成掃描該網域下所有 frame 尋找喚醒按鈕
文字或 `[data-testid="stApp"]`，而非寫死單一 frame 路徑。找到喚醒按鈕就點擊，然後
輪詢直到 app 本體出現或逾時。`keep-alive.yml` 改為安裝 Playwright + Chromium
（`python -m playwright install --with-deps chromium`），跑法與 `shopee-ci-test.yml`
既有的瀏覽器安裝方式一致。

**理由**：對「app 是否醒著」的唯一可信判斷方式，就是做訪客真正做的事——載入頁面、
等 JS 執行。任何不執行 JS 的檢查方式（curl、`HEAD` 請求）看到的都只是「伺服器有回應」，
跟「app 有沒有在跑」是兩件事，這次的第一版正是被這個落差誤導。

**否決的方案**：
- **不做任何處理，僅在 README 已知問題註記**：使用者明確認為「觀感不好」需要實際
  對策，被否決。
- **遷移到 Render / Fly.io / Hugging Face Spaces 等其他平台**：延後——先確認瀏覽器
  版 keep-alive 的實際效果，換平台是更大的決定，不值得在此之前投入。

**代價**：每次執行要下載並啟動一個真實瀏覽器，比原本的 `curl` 版本慢、也多消耗一些
GitHub Actions 執行時間（`timeout-minutes: 10`，實測單次執行遠低於此）；若 Streamlit
的休眠畫面 DOM 結構改版，`WAKE_BUTTON_PATTERN`／`APP_READY_SELECTOR` 這兩個判斷依據
可能需要跟著更新——腳本設計成兩者都找不到時直接回報錯誤而非猜測成功，讓這類改版
會被明確發現而不是被靜靜吞掉。

**驗證**：本機對目前處於清醒狀態的正式 app 執行 `wake_streamlit.py`，確認能在該網域
的巢狀 iframe 中找到 `[data-testid="stApp"]` 並回報「app was already awake」（exit
0）；對不存在的網域執行，確認會回報 `::error::` 並以非 0 結束，不會誤報成功。**尚未
在 app 真正處於休眠狀態時驗證過喚醒按鈕的偵測與點擊**——這條路徑要等 app 自然休眠一次
（或手動讓它休眠）才能實地驗證，屬於待追蹤項目。

**重新評估的條件**：若排程執行數次後 app 仍會被判定為休眠（喚醒按鈕偵測失效，可能是
Streamlit 改了 DOM 結構），需要更新選擇器並重新驗證；若持續觀察後即使喚醒機制正確
運作、6 小時頻率仍不夠，或使用者想要自訂網域／無休眠保證，則需要重新評估遷移到付費層
或其他平台。

---

## 36. 公開授權採用 MIT License

**日期**：2026-09-11　**狀態**：已完成

**背景**：repo 公開後一直沒有 `LICENSE` 檔案，但 README 有一個「授權與注意事項」
標題，內容其實是資安衛生與 robots.txt 遵循說明，不是授權條款——標題名不符實，
容易讓看 repo 的人誤以為專案有明確授權，實際上什麼授權條件都沒有寫清楚。

**決定**：新增 MIT License（`LICENSE` 檔案，著作權人 `SaintY-918`，年份 2026）。
README 原本的「授權與注意事項」拆成兩段：「授權」（連到 `LICENSE`，並附上程式碼
「現狀」提供、評分結果僅供參考的免責聲明）與「安全與使用限制」（原本的資安衛生、
robots.txt 遵循內容）。

**理由**：作為作品集，MIT 是最常見、對閱讀者與潛在雇主/協作者最沒有理解門檻的選擇——
允許自由使用、修改、散布（含商業用途），只需要保留授權聲明。拆分標題是因為原標題
底下沒有任何實際的授權條款，讀者若真的想知道能不能用這份程式碼，找不到答案。

**驗證**：`scripts/check_docs.py` 執行通過，確認沒有引入新的文件不一致；grep 確認
repo 內沒有其他地方引用舊的「授權與注意事項」錨點連結。

---

## 37. logging 設定集中到單一模組

**日期**：2026-09-11　**狀態**：已完成

**背景**：`logging.basicConfig(level=logging.INFO, format="%(levelname)s: %(message)s")`
這一行分別貼在 `src/main.py`、`src/scripts/probe_shopee_affiliate.py`、
`src/scripts/probe_shopee_browser.py`、`src/scripts/suppress_initial_burst.py`
四個進入點裡，格式若要調整需要記得同步改四個地方。

**決定**：新增 `src/utils/logging_setup.py`，提供單一函式 `configure_logging()`，
四個進入點改為呼叫它。其餘模組維持原本的 `logging.getLogger(__name__)`，不呼叫
`basicConfig`——只有真正啟動 process 的進入點才決定 log 格式，這個分工不變。

**理由**：格式只需要改一個地方；也讓「哪些檔案是可以獨立執行的進入點」這件事透過
import 語意變得更明確（會呼叫 `configure_logging()` 的都是進入點）。

**驗證**：`pytest tests/ -q --ignore=tests/e2e` 全過；`ruff check .` 全過（含抓到
`src/scripts/probe_shopee_affiliate.py`、`probe_shopee_browser.py` 因此變成未使用
的 `import logging`，已一併移除）；逐一 `import` 四個進入點模組確認不拋例外。

---

## 38. 支援非 MacBook 的 Mac —— 延後，卡點是資料模型不是資源

**日期**：2026-09-12　**狀態**：同日安排了設計討論，第一階段已實作，見 [#39](#39-支援-mac-mini-與-mac-studio同一份公式分開的級距)

**背景**：專案取名 `mac-valuer`，名字本身暗示「不只 MacBook」。但目前資料模型
（`MacBookSpec`、`ScoringWeights`、`form_factor_key`，見 `.spec/specs/data-models/spec.md`、
`score-engine/spec.md`）與爬蟲過濾條件全部假設物件是筆電：`screen_size`、電池健康度
是筆電特有欄位，`form_factor_key` 只認 Air/Pro 13/Pro 14/16/Neo 這幾種筆電尺寸；
Mac mini／Mac Studio／Mac Pro 沒有螢幕、沒有電池，iMac 有螢幕但邏輯又不一樣。
PTT 標題過濾（`"macbook" in title_lower`，見 `src/scrapers/ptt.py`）與蝦皮/旋轉拍賣
的過濾邏輯同樣寫死只認筆電關鍵字。

**決定**：概念上同意值得做，但**延後**——這不是加一個型號、加一個 if 分支能解決的
小改動，需要先重新設計資料模型：把「這是不是筆電」與「用什麼記分方式」拆開。
`#15`（支援 MacBook Neo）已經記過同一種坑——「家族寫死在過濾邏輯裡」——這次要擴到
完全不同外型的桌機，同樣的假設會在爬蟲標題過濾、LLM 解析 prompt、評分公式三個地方
各犯一次，值得一次專門的設計討論，不是本輪作品集體檢能順手做掉的範圍。

**理由**：設計成本主要來自「桌機沒有 form factor 這種概念可以類比」——筆電的
Air/Pro/尺寸差異直接對應外殼與螢幕品質差異，Mac mini 之間的差異幾乎只剩晶片與埠位
規格，硬套用現有的 `form_weight` 機制沒有意義，需要重新想一套桌機的評分邏輯，或至少
決定桌機是否要沿用同一份 VFM 公式。

**已排除的疑慮（不需要在設計時重新推導）**：使用者原本擔心資料量增加會撐爆免費資源
（Neon DB／Gemini／Streamlit）。查證後結論是**資源不是這個擴充的阻礙**：
- 本機 SQLite 實測一筆物件（title + body_content + parsed_json）平均約 1KB，
  Neon 免費層 0.5GB 換算下來還有數十萬筆的空間，台灣二手 Mac 桌機的流通量遠小於
  筆電，不可能在合理時間內衝到這個量級。
- Gemini 解析成本已經在 `#18` 解決：成本綁的是「新物件數量」而非資料庫總量
  （解析指紋跳過沒變的舊資料），擴品類不會讓往後每晚的成本隨資料庫總量無限成長。
- Streamlit 休眠是流量問題（有沒有訪客），跟資料量無關。
- 唯一實際風險是**一次性歷史回填**：若一口氣把歷史桌機物件全部抓入，短時間可能
  連續撞到 Gemini 每日 500 次上限好幾天——這是一次性、可控的（可用 `--skip-scrape`
  分批處理），不是長期會失控的東西。

**重新評估的條件**：安排專門的設計討論時，至少要先決定：
1. 桌機需要哪些 `MacBookSpec` 沒有的欄位（例如是否含螢幕），哪些欄位（`screen_size`、
   電池健康度）對桌機沒有意義該如何處理（留空？換一組欄位？）。
2. 桌機的評分邏輯要不要沿用 VFM 公式與 `form_weight` 機制，或需要一套獨立公式。
3. 爬蟲標題過濾與 LLM 解析 prompt 如何擴大關鍵字涵蓋 Mac mini／Studio／Pro／iMac，
   同時不誤傷或誤判（例如「mini」這個詞本身在筆電語境也可能出現）。

---

## 39. 支援 Mac mini 與 Mac Studio：同一份公式，分開的級距

**日期**：2026-09-12　**狀態**：已實作（第一階段）

**背景**：#38 延後時列了三個要先回答的問題：桌機缺哪些欄位、要不要沿用 VFM 公式、
爬蟲與 prompt 怎麼擴而不誤傷。這是那場設計討論的結論。

**決定**：

1. **Mac mini 與 Mac Studio 是同一類東西**——沒有螢幕、沒有電池的主機——彼此的差異像
   Air 與 Pro 一樣落在 `series` 這一層。`ModelSeries` 加兩個值，`device_class(series)`
   推導出 `laptop`／`desktop`，**不另存**：它是 `series` 的函數，存第二份就是第二個
   會漂移的來源。`MacBookSpec` 不改名，名稱是歷史遺留。
2. **沿用同一份 VFM 公式**，新增 `form_mini` 1.00、`form_studio` 1.05。桌機的
   `screen_size`、`battery_health` 一律 `None`，解析清理階段強制清空。
3. **兩類的分數不可互比，所以級距分開**：Dashboard 的 p50／p75 從同類物件切、圖例寫
   「筆電基準」／「桌機基準」、API 的 `vfm_thresholds` 按類回傳、Discord 門檻按類設定。
   同類在售少於 10 筆時不分級（#6 對百分位的顧慮）。
4. **Dashboard 用「筆電｜桌機」模式切換，不是篩選**：切換後機型選項、螢幕尺寸、滑桿、
   基準全部跟著換，「重置」不重置模式。
5. **桌機警報先關閉**：`ALERT_VFM_THRESHOLD_DESKTOP` 留空即不推播，累積兩週看 p75 再定。
6. **三個爬蟲共用 `detect_product()`**；蝦皮的搜尋關鍵字改讀 `SHOPEE_KEYWORDS`，
   預設不變，桌機關鍵字由跑本機排程的人決定要不要開。

**理由**：主機沒有螢幕與電池，同一顆晶片每千元買到的效能天生較高——M4 Mac mini 16GB
以全新價 19,900 計算就約 740 分，超過筆電門檻 500。混排等於永遠把桌機放榜首，
沿用門檻會每晚狂響。這是事實不是誤差，不該用乘數去修。

**否決的方案**：

- **用 `form_mini` 之類的乘數把桌機分數壓到筆電尺度**（例如 ×0.7）。那是為了讓數字好看
  而捏造的係數，分數就不能被驗算，違反 #3／#26。
- **桌機另寫一套公式**。違反「公式只能有一份實作」，而且晶片、年份、RAM、SSD 沒有任何
  一項在桌機上意義不同。
- **併入既有「機型」下拉選單混排**。改動最小，但色帶與中位數只能取全站，桌機會系統性
  地衝到榜首。

**延後的部分**：iMac（含 24 吋螢幕、價值組成不同、二手量少，需要第三組級距）與
Mac Pro（幾無二手流通，且「Mac Pro」與「MacBook Pro」比對相撞）。
**重新評估的條件**：桌機在售穩定 ≥ 30 筆，或實際看到 iMac 的需求。

**額度的實測結論**：新增請求預設為 0（PTT 的 feed 本來就含桌機、旋轉拍賣共用同一個
30 筆額度、蝦皮預設關鍵字不變）；Gemini 由既有的每晚 50 次上限與解析指紋（#18）兜底，
首晚 110 筆蝦皮新物件確實撞到上限、多出的 24 筆自動留到隔天。真正會漏氣的一點是
`needs_fix` 對桌機要求 `screen_size`——沒有螢幕的機器永遠「不完整」——已改為依 class 決定
必要欄位。

**上線後第一個小時撞到的**：Streamlit 會把「這一輪沒渲染的 widget」的 session key 刪掉，
切到桌機後筆電滑桿沒畫，下一次重跑就 `KeyError`。切換本身有 e2e 測試，切換後的下一個動作
沒有。兩組權重改存在不綁 widget 的 session dict，並補了「切換 → 篩選 → 切回」的測試。

**通則**：**一個新類別進來，先問「哪些地方假設了舊類別」**——這次是形態加成的尺寸預設、
解析迴圈的必要欄位、色帶的全站基準、警報門檻、三個爬蟲的標題過濾，五個地方各假設一次
「Mac 就是筆電」。

---

## 40. 一個標題同時出現 Apple 晶片與 Intel 世代訊號，就不是一台機器

**日期**：2026-09-12　**狀態**：已實作

**背景**：桌機上線當天，VFM 全站第一是一台 5,000 元的 Mac mini M1，1242 分。原始標題：
`真猛電腦 現貨mac mini m1 2012 2014/i7/16g/500g ssd+ 4tb`——店家一則賣好幾代的「現貨」
貼文，5,000 是 2012 年那台的價格。三道防線同時失守：

1. `i7/` 後面接斜線，Intel 標記的 regex 只認空白或連字號。
2. 標題寫了 2012、2014，但年份由「M1 → 2020」推論蓋掉，「早於 2020 就丟棄」的規則
   （#4 那條）沒機會生效。
3. 蝦皮精簡模式只看得到最便宜規格的價格。

**決定**：

- `i5/i7` 後接 `/`、`,`、`、` 或句尾也算 Intel 標記。
- 新增 `PRE_SILICON_YEAR`：標題裡出現 2006–2019 的年份直接否決晶片，邊界規則與
  `CHIP_RE` 相同（`A2337` 型號、「保固至2025」不受影響）。
- 蝦皮精簡模式加 `SHOPEE_MAX_VARIANT_SPREAD`（預設 1.5）：`price_max` 超過最低價 1.5 倍
  代表同一標題底下是不同機器，整筆略過；範圍內者保留並在內文註記「價格區間」。
- `revalidate_chips.py` 學會年份規則，對全庫重驗只抓到這一筆，已清掉。

**理由**：混合世代的貼文沒有「一台機器」可以評分。丟棄只是少一次機會；以最好的機器之名、
最差的機器之價評分，會發出假的撿漏警報，後者代價大得多（同 #9、#15 的方向）。
掃過全庫 447 筆標題，同時含 Apple 晶片與任一 Intel 訊號的只有這一筆，新規則不會誤傷。

**否決的方案**：對多規格商品改抓詳情頁做 Variant Flattening——資料會正確，但每筆多一次
頁面載入，正是精簡模式要避免的被判為爬蟲的特徵；先用價差門檻擋，等出現漏網再評估。

**通則**：**推論出來的值不能反過來蓋掉賣家明寫的訊號。** 年份推論是為了補缺值，
不是為了改寫標題。

---

## 41. 賣場範本的分級說明不算機況

**日期**：2026-09-12　**狀態**：已實作

**背景**：一台全新未拆的 MacBook Neo 被標成瑕疵。內文是店家貼在每一則商品底下的
「商品狀態分級說明」，其中一行「● 70%新 = 商品瑕疵或功能異常」命中了「瑕疵」。
同一家店在資料庫裡有 6 筆，全部乾淨、全部被標。

**決定**：凡是「兩到三位數%新」後面接 `=`／`：` 的整行，掃描前移除——那是定義，
不是描述。商品自己的「【新舊程度】全新品 / 100%新」沒有等號，保留；真的寫
「70%新，商品瑕疵」來描述機器的仍會被標。

新增 `revalidate_defects.py`：瑕疵是解析時算好存進 `parsed_json` 的（#13），規則改了
舊資料不會自己跟上（#23）；這支腳本不用 LLM 就能重算全庫，經 `update_parsed` 寫回，
不動 `last_seen`（#16）。試算正好抓到那 6 筆，已寫回。

**理由**：#3 說過，錯標一台好機器的代價是讀者學會忽略徽章。這裡的誤標來源是範本文字，
它會出現在該店每一則商品裡，不處理就是整家店永遠帶徽章。

**否決的方案**：把「瑕疵」從關鍵字拿掉——它是最常見的真實描述（「瑕疵機」）；
或只掃標題不掃內文——#13 已經記過，PTT 賣家正是把機況寫在內文。

**通則**：**內文裡的通用範本要在掃描前剝掉。** 分級說明是第一種；日後看到交易條款、
退貨規則之類的段落誤觸關鍵字，用同樣的方式處理，不要放寬關鍵字。


---

## 43. 標題級的否決必須獨立於「晶片從哪裡來」

**日期**：2026-09-16　**狀態**：已實作

**背景**：#40 結尾寫著那筆 5,000 元的 mac mini「已清掉」。四天後它又是桌機榜首。
資料庫裡的時間欄位說明了經過：`first_seen` 9/12（#40 當天進來）、`updated_at`
9/13 02:41——**清掉的隔晚，排程把它解析回來了**。

它繞過了 #40 建立的每一道防線，而且不是因為規則寫錯：

- `force_extract_chip()` 確實拒絕這個標題，但 Step 2 **只在 LLM 沒給晶片時才呼叫它**。
  LLM 從同一個標題讀出了 `M1`，於是它根本沒被問。
- 「年份早於 2020 就丟棄」也失效：`infer_correct_year()` 依桌機表把 Mac mini + M1
  改寫成 2020，年份欄位乾乾淨淨。
- 爬蟲層沒有任何 Intel 判定（蝦皮 L1 只問 `detect_product()`），所以這一列每次
  重新抓取都會再度進入資料庫。

**決定**：

- 把 #40 的兩條規則抽成 `is_intel_era_title(title)`，讓「標題說了 Intel」可以
  獨立於晶片抽取被詢問。
- Step 2 改用 `_is_intel_era_row(chip, year, title)`，**同時**問年份與標題。
- 蝦皮 L1 加上同一道檢查，把這種標題擋在資料庫之外。
- 詳情頁模式打平變體時，對組合後的 `商品名 - 規格名` 再檢查一次（見下）。
- 對正式資料庫執行 `revalidate_chips.py --apply`，清掉這一筆與另一筆
  `MacBook Pro（16 吋，2019 年）M1 Pro`。

**理由**：規則寫在哪裡，決定了它能不能生效。`force_extract_chip()` 是**抽取器**，
只有在需要抽取時才會被呼叫；把否決藏在抽取器裡，等於宣告「LLM 成功時不必檢查」——
而 LLM 讀的是同一個標題，它犯的正是這個規則要防的錯。**否決條件必須與產生值的
路徑分開，否則它只保護其中一條路。**

**蝦皮標題與規格選項是兩段文字。** 打平後的標題是 `商品名 - 規格名`，L1 只看過商品名。
店家真正在賣的機器寫在**選項**裡：標題掛「MacBook Pro M1」，選項卻是「2017 i5」與
「2020 M1」。檢查商品名等於只讀了標題，沒讀商品。

**否決的方案**：

- **蝦皮 L1 改用 `mentions_apple_silicon()`**（與 PTT 一致）。它對「標題根本沒寫晶片」
  也回 False，但那種商品的描述裡可能有晶片、詳情頁模式下 LLM 讀得到——用它會把
  「沒講」和「講了是 Intel」當成同一件事。採用正面訊號（`is_intel_era_title()`），
  只擋確定的那一種。
- **把解析指紋寫進被丟棄的空列**，以省下每晚的重複呼叫。這會產生一個只有 hash
  的 `parsed_json`，而 Step 3 讀取所有 `parsed_json` 非空的列——`main.py` 裡已有
  註解說明不可這麼做。代價是每晚 2 次 LLM 呼叫（額度 50），有界，暫時接受。

**未處理**：旋轉拍賣的過濾（`carousell.py`）同樣只問 `detect_product()`，有相同缺口。
目前沒有實際案例，一行即可補上。

**重新評估**：若被清掉的列再度出現，或旋轉拍賣出現同類資料，即補上述缺口並重新
檢視「清空 `parsed_json`」是否該改為刪除該列。

**通則**：**「已清掉」不是一個終局狀態。** 只要產生它的路徑還在，資料就會自己長回來——
驗證清除是否有效，要看的是下一次排程之後，不是執行當下（呼應 #23）。

---

## 42. 蝦皮要的是「最近有人通過驗證」，不是 session，也不是瀏覽器模式

**日期**：2026-09-13　**狀態**：已確認（實測）

**背景**：Discord 連日回報蝦皮失敗。9/12 手動重新登入後成功，9/13 排程又失敗，
而且網址從 `verify/traffic/error?...is_logged_in=false` 變成
`verify/captcha?...scene=crawler_item`——和 #1 裡 GitHub runner 被封時同一種標記。

**結論**：能不能抓到資料，只取決於**最近是否有人通過滑動驗證**。
與 headless 無關，與請求數無關，與 session 檔的年齡無關。

**實測（2026-09-13 10:24–10:30，住宅 IP，同一個 session 檔）**：

| 順序 | 條件 | 結果 |
|---|---|---|
| 1 | 有頭，開啟搜尋頁 | 被導向驗證頁 |
| 2 | **人工滑掉拼圖**，回到搜尋頁 | 60 筆 |
| 3 | 無頭，同一頁，數分鐘後 | **60 筆** |
| 4 | 無頭，完整 9 個搜尋頁 | **每頁 60 筆，100 筆通過過濾** |

第 3 步就推翻了 headless；第 4 步推翻了「請求數過多會被標記」。

**驗證的有效期很短。** 8/29 解一次 → 當天成功，8/31 起全數失敗；
9/12 解一次 → 當天成功，9/13 失敗。約一到兩天就要再解一次。

**這條路徑因此無法完全無人值守**，而且沒有任何設定能改變這件事——
蝦皮要的就是一個人。排程能做的只有失敗時把話說清楚。

**先前兩個錯誤的判斷，都記在這裡當教訓**：

1. **「session 壽命約 1-2 週」**。依據是 9/11 那天 12.8 天的 session 才失效，
   但 9/7 與 9/11 兩天其實是 `DBManager()` 連線就拋例外，根本沒跑到蝦皮。
   **沒有錯誤訊息不等於成功。**
2. **「headless 是觸發條件」**。當時看到「有頭 60 筆、接著無頭被擋」就下結論，
   並據此把排程改成有頭。實際上那兩次之間還有一個我看不到的變因——人在螢幕前
   滑掉了拼圖。**跑在別人機器上的有頭瀏覽器，人的動作不會出現在我的日誌裡。**
   有頭的改動已回退，排程維持 `SHOPEE_HEADLESS=true`。

**保留的改動**：`SHOPEE_INTERACTIVE`（預設 `true`，排程設 `false`）。它與 headless
是兩件事：排程開的視窗一樣沒人看著，撞牆時必須拋 `ShopeeSessionExpired` 通報 Discord，
而不是停在 `input()` 等到一小時逾時被殺——那樣 Discord 什麼都不會說。
例外訊息也改寫為「需要人通過滑動驗證」，不再說「session 過期」——後者會讓人去重登，
但登入從來不是問題。

**否決的方案**：固定 Camoufox 指紋、持久化 profile、改用有頭。三者都不影響
蝦皮要不要人工驗證。

**處理方式：把人工步驟移到人確實在場的時刻。** 排程在凌晨 02:30 跑，那時沒有人，
所以它注定失敗。新增 `mac-valuer-shopee-verify` 登入排程：登入後 3 分鐘先用**無頭**
確認驗證是否還有效，有效就靜靜結束，失效才跳出視窗要人滑拼圖。早上解掉，當晚
02:30 大約是 17 小時後，落在觀察到的有效期內。

**先探測再決定要不要開視窗，是這個設計唯一重要的地方。** 每次登入都硬跳視窗的
東西，一週內就會被關掉，然後排程又回到半夜無聲失敗——而那正是這整則紀錄的起點。

**重新評估**：Affiliate API（#2）開通後瀏覽器路徑整個退場，這題自動消失。
在那之前，蝦皮資料本質上是「有人顧的那幾天才有」，排程失敗屬於預期行為，
不應視為故障。
